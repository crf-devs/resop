{% extends 'organization/base.html.twig' %}

{% block javascripts %}
    {{ parent() }}
    {{ encore_entry_script_tags('missions') }}
{% endblock %}

{% block title %}{{ 'organization.mission.editFormTitle' | trans }}{% endblock %}

{% block body %}
    <a class="btn btn-outline-danger trigger-delete float-right" data-display-name="{{ mission.name }}" href="#" data-href="{{ path('app_organization_mission_delete', { id: mission.id }) }}">{{ 'action.delete' | trans }}</a>

    <a href="{{ path('app_organization_mission_index') }}">{{ 'common.backToList' | trans }}</a>

    <h1>{{ 'organization.mission.editFormTitle' | trans }}</h1>

    {{ include('organization/mission/_form.html.twig', {'button_label': 'action.save' | trans}) }}
{% endblock %}
