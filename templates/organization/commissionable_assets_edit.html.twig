{% extends 'organization/base.html.twig' %}

{% block title %}Edition d'un véhicule{% endblock %}

{% block body %}
    <h1>Edition d'un véhicule</h1>
    {{ form_start(form) }}
    {{ form_errors(form) }}
    {{ form_row(form.type, {label: 'Type'}) }}
    {{ form_row(form.name, {label: 'Nom'}) }}
    {{ form_end(form) }}
{% endblock %}
